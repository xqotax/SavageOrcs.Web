@page
@model RegisterModel
@{
    Layout = "~/Areas/Identity/Pages/_AuthLayout.cshtml";
}

@*<div class="row">
    <div class="col-md-4">*@
<form id="registerForm" asp-route-returnUrl="@Model.ReturnUrl" method="post">
    <div class="row">
        <div class="col-md-10 offset-md-1 top-buffer">
            <h2>Створення нового акаунту</h2>
        </div>
    </div>
    <div class="row">
        <div class="col-md-7 offset-md-1 top-buffer">
            <p style="color: red">
                Увага, дана сторінка тільки для реєстрації адмінів. Навіть якщо ви зареєструєтесь,
                у вас не буде можливості користуватись адмінською системою, поки вам не нададуть відповідні права
            </p>
        </div>
    </div>
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div class="row top-buffer">
        <div class="col-md-7 offset-md-1">
            <label asp-for="Input.FirstName"></label>
            <input asp-for="Input.FirstName" class="form-control" autocomplete="firstname" aria-required="true" />
            <span asp-validation-for="Input.FirstName" class="text-danger"></span>
        </div>
    </div>
    <div class="row top-buffer">
        <div class="col-md-7 offset-md-1">
            <label asp-for="Input.lastName"></label>
            <input asp-for="Input.lastName" placeholder="Не обов'язкове" class="form-control" autocomplete="lastname" aria-required="true" />
            <span asp-validation-for="Input.lastName" class="text-danger"></span>
        </div>
    </div>
    <div class="row top-buffer">
        <div class="col-md-7 offset-md-1">
            <label asp-for="Input.Email"></label>
            <input asp-for="Input.Email" placeholder="Використовується для входу" class="form-control" autocomplete="username" aria-required="true" />
            <span asp-validation-for="Input.Email" class="text-danger"></span>
        </div>
    </div>
    <div class="row top-buffer">
        <div class="col-md-7 offset-md-1">
            <label asp-for="Input.Password"></label>
            <input asp-for="Input.Password" class="form-control" autocomplete="new-password" aria-required="true" />
            <span asp-validation-for="Input.Password" class="text-danger"></span>
        </div>
    </div>
    <div class="row top-buffer">
        <div class="col-md-7 offset-md-1">
            <label asp-for="Input.ConfirmPassword"></label>
            <input asp-for="Input.ConfirmPassword" class="form-control" autocomplete="new-password" aria-required="true" />
            <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
        </div>
    </div>
    <div class="row top-buffer bottom-buffer">
        <div class="col-md-5 offset-md-1">
            <button id="registerSubmit" type="submit" class="w-100 btn btn-lg btn-dark">Зареєструватись</button>
        </div>
    </div>
</form>
    @*</div>
    <div class="col-md-6 col-md-offset-2">
        <section>
            <h3>Use another service to register.</h3>
            <hr />
            @{
                if ((Model.ExternalLogins?.Count ?? 0) == 0)
                {
                    <div>
                        <p>
                            There are no external authentication services configured. See this <a href="https://go.microsoft.com/fwlink/?LinkID=532715">article
                            about setting up this ASP.NET application to support logging in via external services</a>.
                        </p>
                    </div>
                }
                else
                {
                    <form id="external-account" asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="form-horizontal">
                        <div>
                            <p>
                                @foreach (var provider in Model.ExternalLogins)
                                {
                                    <button type="submit" class="btn btn-primary" name="provider" value="@provider.Name" title="Log in using your @provider.DisplayName account">@provider.DisplayName</button>
                                }
                            </p>
                        </div>
                    </form>
                }
            }
        </section>
    </div>
</div>*@

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
